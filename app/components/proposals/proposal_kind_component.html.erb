<!-- app/components/proposals/proposal_kind_component.html.erb -->
<div>
  <%= form.label :proposal_kind_id, "Project Type / Scope" %>
  <p class="help-text">Select which project or matching system this proposal belongs to.</p>

  <%= form.select :proposal_kind_id, proposal_kind_options,
                  { prompt: "Please select a category..." },
                  { label: false } %>
</div>
