<%= render "shared/globalize_locales", resource: milestone %>

<%# Create the form tag with the explicit nested path for users %>
<%= translatable_form_for [@budget, @investment, milestone] do |f| %>
  <%# Render the shared fields, passing in the form builder object 'f' %>
  <%= render 'shared/milestones/form_fields', form: f %>
<% end %>