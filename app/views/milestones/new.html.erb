<div class="milestone-new row">
  <div class="small-12 column">
    <%# This calls the method in your controller which now knows about @budget/@investment %>
    <%= back_link_to milestoneable_path %>

    <h1><%= t("admin.milestones.new.creating") %></h1>

    <%# Pass the milestone object explicitly to the partial %>
    <%= render "form", milestone: @milestone %>
  </div>
</div>
